<html>

<head>
  <title>URL Shortener</title>
  <link href="https://fonts.googleapis.com/css?family=Playfair+Display" rel="stylesheet">
  <link href="/assets/styles.css" rel="stylesheet" type="text/css" />
</head>

<body>
  <div id="main">
    <h1>URL Shortener</h1>
    <form id="urlForm" method="POST" action="/">
      <input type="text" name="url" placeholder="" required/>
      <button type="submit">></button>
    </form>
    <p>
      <% if (!data.message && !data.shortenedUrl) { %>
        "Why say lot word when few do trick?"
      <% } else if (!data.shortenedUrl) { %>
        <%= data.message %>
      <% } else { %>
        <a href='<%= data.shortenedUrl %>'><%= data.shortenedUrl %></a>
      <% } %>
    </p>
  </div>
</body>
</html>
